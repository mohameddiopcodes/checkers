const updateBoard = (e, cellId) => {
    //rerenderingBoard


    // if(e.target.childNodes[0]) {
    //     flatBoard[cellId] = turn;
    //     flatBoard[cellId-turn*9] = 0;
    //     turn *= -1;
    //     render();
    // } else if(e.target.childNodes[0]) {
    //     flatBoard[cellId] = turn;
    //     flatBoard[cellId+turn*11] = 0
    //     turn *= -1;
    //     render();
    // }
}

module.exports = updateBoard